dataset:
  slides_dir: "dataset/slides"
  annotations_dir: "dataset/annotations"

results_dir: "results"

pipeline:
  patch_size: 256
  step_size: 256
  thumbnail_downscale: 64

model:
  feature_extractor: "resnet50"
  mil_model: "clam_sb"

training:
  batch_size: 256
  epochs: 100
  lr: 0.0001
